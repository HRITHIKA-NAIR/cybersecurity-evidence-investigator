GEMINI_API_KEY=
VIRUSTOTAL_API_KEY=
FRONTEND_ORIGIN=
DATABASE_URL=
DB_POOL_MIN=1
DB_POOL_MAX=5
DB_POOL_MAX_WAITING=20

# Auth - generate with:
#   python -c "import secrets; print(secrets.token_urlsafe(48))"
# Required, at least 32 random characters. Never commit a real value.
JWT_SECRET=
